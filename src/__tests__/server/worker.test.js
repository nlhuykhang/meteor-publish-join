/* global before, after */

import { expect } from 'chai';
import sinon from 'sinon';

import {
  startPublishWorker,
  stopPublishWorker,
} from '../../server/worker';

const { describe, it } = global;

describe('worker function', () => {});
